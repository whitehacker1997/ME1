using System.Linq.Expressions;

namespace OnApp.BizLayer.Extensions
{
    /*public class SortFilterExtension<T>
        where T : class
    {
        public static IQueryable<T> SortFilterSearchExtension(
            this IQueryable<T> query,
            string search)
        {
            var predicateExpression = null as Expression<Func<T, string, bool>>;

            predicateExpression = (T enity,string search) 
                                        => T.
            return query;
        }
    }*/
}
